import { useRecoilState } from 'recoil';

function useSelectSheetList() {
  const selectSheetByID = (sheetID: string) => {};
  const selectSectionByID = (sheetID: string, sectionID: string) => {};
  const selectItemByID = (
    sheetID: string,
    sectionID: string,
    itemID: string
  ) => {};

  return {};
}
